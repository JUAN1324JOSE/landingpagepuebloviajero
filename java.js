function scrollToSection(id) {
    document.getElementById(id).scrollIntoView({ behavior: 'smooth' });
}

function initMap() {
    var location = { lat: -25.363, lng: 131.044 }; // Ubicación inicial
    var map = new google.maps.Map(document.getElementById)('map') }
        zoom
        